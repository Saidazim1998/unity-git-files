1. git init
2. git lfs install
3. git lfs track ".*psd"
4. replace .gitattrbutes file (important)
5. touch .gitignore
6. vi .gitignore (press I key write /Logs/ and press esc then write :wq)
7. replace gitignore file
8. git add -A
9. git commit -am "initial commit"
10. git brach -m master main
11. git remote add origin "origin link"
12. git push -u origin main
